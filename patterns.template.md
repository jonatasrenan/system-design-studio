# Patterns

Decision patterns that repeat across designs, each with the defense already worked out. Sibling of `learnings.md`: that file holds gaps that keep recurring, this one holds decisions already resolved.

Format of each entry:

```
## <decision pattern> (e.g.: SQL vs. KV for the source of truth)
- **Choice**: what was chosen, in general terms (not tied to one session's specifics).
- **When it changes**: what about the problem would flip this decision.
- **30s defense**: the ready articulation, with the nuance that makes the difference.
- **Seen in**: sessions/<slug> (date)
```

---

_(empty — the first entries appear once a decision pattern repeats across sessions)_
